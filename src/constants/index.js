const { ALL_ROLES } = require("./roles");
const { USERS } = require("./users");
const MUSCLE_GROUPS = require("./muscle_groups");

//export constants
module.exports = {
    ALL_ROLES,
    USERS,
    MUSCLE_GROUPS
}